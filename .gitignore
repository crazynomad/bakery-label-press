# Build output
dist/
data/_release-input.csv

# Python
.venv*/
__pycache__/
*.pyc
*.pyo

# OAuth helper output (never commit)
scripts/run-release-local.sh

# Usage-guide exports — .md is the single source of truth, regenerate as needed
docs/usage.*.pdf
docs/usage.*.png

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Client-provided source PDFs + intermediate extraction artefacts. The
# curated data/master-catalog.csv is the single source of truth the Sheet
# ingests; re-run scripts/extract-pdf-labels.py against a local PDF copy
# to regenerate.
data/New Etiquetas both sides/
data/extracted/
